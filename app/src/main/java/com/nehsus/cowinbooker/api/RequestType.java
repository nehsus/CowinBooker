package com.nehsus.cowinbooker.api;

/**
 * RequestType is the enum that is used to differentiate between API methods.
 *
 * @author Sushen Kumar
 */
public enum RequestType {
    GET, POST;

    RequestType() {
    }
}